import React from 'react';

const GlobalCase = () => {
    return (
        <div style={{ margin:'30px auto 20px auto', display:'block' }}>
            <input type="text" name="country"/>
            <button>Submit</button>
        </div>
    );
}

export default GlobalCase;